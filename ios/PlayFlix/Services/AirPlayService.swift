import Foundation

struct AirPlayService {
    func starterChecklist() -> [String] {
        [
            "Add AVRoutePickerView to the native player scene.",
            "Mirror the current playback item into an AVPlayer-based controller.",
            "Handle remote route changes and playback handoff."
        ]
    }
}
