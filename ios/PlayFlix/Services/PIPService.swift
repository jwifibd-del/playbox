import Foundation

struct PIPService {
    func starterChecklist() -> [String] {
        [
            "Adopt AVPlayerViewController for video playback.",
            "Enable Picture in Picture on supported player scenes.",
            "Sync playback state when the app moves between foreground and PiP."
        ]
    }
}
